<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AaharKendra" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AaharKendra" parent="Base.Theme.AaharKendra" />

    <style name="Spinner" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:dropDownSelector">@color/black</item>
        <item name="android:entries">@array/designations</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:textAppearance">@style/SpinnerItemText</item>
    </style>

    <style name="SpinnerItemText" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:textColor">@color/black</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.MaterialComponents.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="Theme.MaterialComponents.NoActionBar" parent="Base.Theme.MaterialComponents.NoActionBar" />

</resources>